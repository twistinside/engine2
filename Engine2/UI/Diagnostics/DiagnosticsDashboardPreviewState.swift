#if DEBUG
/// Representative fixture states for dashboard preview iteration.
enum DiagnosticsDashboardPreviewState: CaseIterable {
    case empty
    case healthy
    case backlog
    case renderError
}
#endif
