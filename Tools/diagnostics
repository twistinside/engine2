#!/usr/bin/env python3
"""Stable command entry point for Engine2 diagnostic artifacts."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent))

from diagnostics_lib.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
