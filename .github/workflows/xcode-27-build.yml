name: Xcode 27 Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: xcode-27-build-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build products
    runs-on: xcode-27
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Report Xcode version
        run: xcodebuild -version

      - name: Ensure Metal toolchain is installed
        run: |
          if ! xcodebuild -showComponent MetalToolchain -json \
            | jq -e '.status == "installed"' > /dev/null; then
            xcodebuild -downloadComponent MetalToolchain
          fi

      - name: Build Engine2
        run: |
          xcodebuild \
            -project Engine2.xcodeproj \
            -scheme Engine2 \
            -configuration Debug \
            -destination 'generic/platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Build Star System Explorer for testing
        run: |
          xcodebuild \
            -project Engine2.xcodeproj \
            -scheme StarSystemExplorer \
            -configuration Debug \
            -destination 'generic/platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build-for-testing

      - name: Build Headless Simulation
        run: |
          xcodebuild \
            -project Engine2.xcodeproj \
            -scheme 'Headless Simulation' \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Verify headless linked dependencies
        run: |
          headless_product="$RUNNER_TEMP/DerivedData/Build/Products/Release/HeadlessSimulation"
          if [[ ! -x "$headless_product" ]]; then
            echo "HeadlessSimulation is missing or is not executable." >&2
            exit 1
          fi

          linked_libraries="$(otool -L "$headless_product")"
          printf '%s\n' "$linked_libraries"
          unexpected_frameworks="$(
            awk '
              NR > 1 && $1 ~ "^/System/Library/Frameworks/" && $1 !~ "/Foundation.framework/" {
                print $1
              }
            ' <<< "$linked_libraries"
          )"
          if [[ -n "$unexpected_frameworks" ]]; then
            echo "HeadlessSimulation linked unexpected frameworks:" >&2
            printf '%s\n' "$unexpected_frameworks" >&2
            exit 1
          fi

      - name: Notify via Pushover
        if: ${{ always() && github.event_name == 'push' && github.ref == 'refs/heads/main' && env.PUSHOVER_TOKEN != '' && env.PUSHOVER_USER != '' }}
        env:
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
          JOB_STATUS: ${{ job.status }}
          GIT_SHA: ${{ github.sha }}
        run: |
          short_sha="${GIT_SHA:0:8}"
          if [ "${JOB_STATUS}" = "success" ]; then
            title="Engine2 build"
            message="Build passed for ${short_sha} on main."
          else
            title="Engine2 build failed"
            message="Build failed for ${short_sha} on main. Check logs."
          fi
          curl --fail-with-body --silent --show-error \
            --form-string "token=${PUSHOVER_TOKEN}" \
            --form-string "user=${PUSHOVER_USER}" \
            --form-string "title=${title}" \
            --form-string "message=${message}" \
            https://api.pushover.net/1/messages.json

      # The preview runner hosts macOS 26, which cannot execute macOS 27 tests.
      - name: Run Engine2 tests (requires macOS 27 runner)
        if: ${{ false }}
        run: |
          xcodebuild \
            -project Engine2.xcodeproj \
            -scheme Engine2 \
            -configuration Debug \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            test
